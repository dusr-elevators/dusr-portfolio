# Base URL for the public API as seen by the browser. Leave empty to use the
# same origin (the contact form posts to /api/... through nginx).
NEXT_PUBLIC_API_BASE_URL=

# Base URL Next uses to reach Django for server-side fetches (SSR/ISR).
# In Docker this is the internal service, e.g. http://backend:8000
API_INTERNAL_URL=
